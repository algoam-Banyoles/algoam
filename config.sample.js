// Opcional: copia aquest fitxer a config.js per sobreescriure el proxy CORS
// que canal.js fa servir per defecte (https://api.codetabs.com/v1/proxy?quest=).
// canal.js fa encodeURIComponent del target URL, així que el proxy ha
// d'acceptar la URL com a query string. Ja no cal cap clau d'API.
// window.APP_CONFIG = { CORS_PROXY: 'https://el-teu-proxy/?' };
